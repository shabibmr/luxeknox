# V3-10 — ADR-0008 object storage (MEDIA trigger)

| | |
| :--- | :--- |
| **Status** | completed |
| **Depends** | V2 exit (parallel-safe with early V3) |
| **Files** | ≤7 |

## Why

ADR-0005 deferred MEDIA until vertical 3 member onboarding needs ID proof / waiver capture.
Write **ADR-0008** accepting storage so V3-11 can implement signed PUT/GET. No BLOB in MySQL
(FR-API-013).

## Files

- `docs/adr/0008-object-storage-and-media.md` (new)
- `docs/adr/README.md` (index entry)
- Possibly update ADR-0005 status pointer to “superseded / triggered by 0008”
- `docs/openapi/v1.yaml` — do **not** flip `/media/*` here (that is V3-11)

## Work

Decide and document:

- Interface: S3 API–compatible (local disk adapter for dev + S3/MinIO for deploy) **or** explicit
  chosen pair — match FR-MEDIA-001..004 and NFR-005 from ADR-0005 parked list.
- Signed PUT upload then attach object key to parent entity (not raw bytes through Nest).
- Signed GET with parent row-level authz; purpose limits (size/MIME) for `avatar`, `id_proof`,
  `waiver`, `medical_cert`.
- BR-HEALTH-001: trainers never receive identity-proof files.
- Orphan collection / retention at least sketched.
- Local verification path without requiring cloud credentials.

## Done when

- ADR-0008 accepted (status Accepted) with clear adapter boundary for StorageService.
- ADR index updated; ADR-0005 points forward.
- No runtime code required in this ticket.
